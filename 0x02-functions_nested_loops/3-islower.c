#include "main.h"

/**
* _islower - does my stuff
* @c - kys
* Description: Does my stuff explained.
* Return: always 0 always void
*/
int _islower(int c)
{
	if (c >= 97 && c < 123)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
